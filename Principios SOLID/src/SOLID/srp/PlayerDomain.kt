package SOLID.srp

import SOLID.srp.Player

class PlayerDomain {
    fun winPercentage(player: Player): Double{
        return 0.0
    }

    fun losePercentage(player: Player): Double{
        return 0.0
    }
}